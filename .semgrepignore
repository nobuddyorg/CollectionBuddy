# Opengrep's own suppression mechanism (its default ignore-file name --
# v1.30.0's CLI has no --semgrepignore-filename to rename it) -- the single
# reviewable place for accepted findings/exclusions, per TEST_STRATEGY.md.
# Every entry needs a reason; do not add a blanket exclusion to silence a
# real finding.
#
# Build artifacts (out/, .next/, coverage/, reports/, .stryker-tmp/,
# test-results/, playwright-report/, node_modules/) are not listed here --
# Opengrep already restricts itself to git-tracked files, and all of those
# are gitignored (web/.gitignore).

# Generated by `supabase gen types`; excluded from every other static-
# analysis tool the same way (eslint.config.mjs, .dependency-cruiser.mjs)
# and kept honest by ci.yml's schema-diff check instead -- see CLAUDE.md
# guardrail #3.
web/src/app/data/database.types.ts
