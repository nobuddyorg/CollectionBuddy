# Opengrep's own ignore file -- v1.30.0's CLI has no flag to rename it off the Semgrep default.
# Every entry needs a reason; never add a blanket exclusion to silence a real finding.

# Generated by `supabase gen types`; excluded from ESLint and depcruise the same way, and kept honest by ci.yml's schema-diff check.
web/src/app/data/database.types.ts
